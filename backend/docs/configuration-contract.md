# Configuration contract

Generated by `bun backend/scripts/configuration-reference.ts --write`.
Verify with `bun backend/scripts/configuration-reference.ts --check`.
The generator validates the examples using the server's configuration rules.

## Deployment settings

The settings below are parsed before database initialization. Only absent values
inherit defaults. Invalid explicit values fail startup. Environment changes require
a process restart. Millisecond timeouts are bounded to avoid timer overflow.
Unknown names in application-owned namespaces such as TERRENCE_, SYSTEM_API_, and
RATE_LIMIT_ fail startup. Unrelated process variables remain available.

| Name | Default | Accepted values |
| --- | --- | --- |
| AGENT_HEARTBEAT_TIMEOUT_MS | 60000 | Integer 1–2147483647 |
| ALLOW_TOOL_FALLBACK | false | true, false, 1, or 0 |
| ALLOW_UNVERIFIED_CHECKSUMS | false | true, false, 1, or 0 |
| AUDIT_STRICT | false | true, false, 1, or 0 |
| AVATAR_CACHE_MAX_AGE_MS | 2592000000 | Integer 1–31536000000 |
| AVATAR_CACHE_MAX_BYTES | 67108864 | Integer 1–10737418240 |
| AVATAR_CACHE_MAX_ENTRIES | 2048 | Integer 1–1000000 |
| CLI_TOKEN_TTL_MS | 2592000000 | Integer 1–31536000000 |
| GC_GRACE_PERIOD_DAYS | 7 | Integer 0–36500 |
| HEALTH_ASSESSMENT_CONCURRENCY | 2 | Integer 1–1024 |
| HEALTH_ASSESSMENT_INTERVAL_MS | 86400000 | Integer 1–2147483647 |
| IACT_QUERY_TOKEN_ENABLED | false | true, false, 1, or 0 |
| INFRACOST_ENABLED | false | true, false, 1, or 0 |
| LOG_CAPABILITY_TTL_SECONDS | 172800 | Integer 1–604800 |
| MIGRATION_CHECKPOINT_RETRIES | 15 | Integer 1–100 |
| MIGRATION_DRAIN_TIMEOUT_MS | 1800000 | Integer 1–2147483647 |
| MIGRATION_SKIP_DRAIN | false | true, false, 1, or 0 |
| PORT | 3000 | Integer 1–65535 |
| RATE_LIMIT_MAX | 60 | Integer 1–1000000 |
| RATE_LIMIT_METRICS_MAX | 30 | Integer 1–1000000 |
| RATE_LIMIT_SCIM_MAPPING_MAX | 10 | Integer 1–1000000 |
| RATE_LIMIT_SCIM_SETTINGS_MAX | 20 | Integer 1–1000000 |
| RATE_LIMIT_SENSITIVE_MAX | 5 | Integer 1–1000000 |
| RATE_LIMIT_SSO_GET_MAX | 60 | Integer 1–1000000 |
| RATE_LIMIT_WORKSPACE_RUN_HISTORY_DURATION_MS | 60000 | Integer 1–2147483647 |
| RATE_LIMIT_WORKSPACE_RUN_HISTORY_MAX | 120 | Integer 1–1000000 |
| RUN_TASK_TIMEOUT_MS | 3600000 | Integer 1–2147483647 |
| SIGNED_URL_TTL_SECONDS | 300 | Integer 1–604800 |
| SIMULATED_RUNS | false | true, false, 1, or 0 |
| SIMULATED_STACK_DEFERRED | false | true, false, 1, or 0 |
| SIMULATED_STACK_PLAN_CHANGES | false | true, false, 1, or 0 |
| SYSTEM_API_PORT | 8443 | Integer 1–65535 |
| TERRENCE_ADMIN_PASSWORD_RESET | false | true, false, 1, or 0 |
| TERRENCE_AGENT_FORWARD_TIMEOUT_MS | 60000 | Integer 1000–300000 |
| TERRENCE_ALLOW_INSECURE_OAUTH_URLS | false | true, false, 1, or 0 |
| TERRENCE_ALLOW_INSECURE_RUN_TASK_URLS | false | true, false, 1, or 0 |
| TERRENCE_ALLOW_PRIVATE_URLS | false | true, false, 1, or 0 |
| TERRENCE_ALLOW_PRIVATE_VCS_URLS | false | true, false, 1, or 0 |
| TERRENCE_ASSESSMENT_POLL_MS | 60000 | Integer 5000–2147483647 |
| TERRENCE_AUTO_DESTROY_POLL_MS | 30000 | Integer 5000–2147483647 |
| TERRENCE_BINARY_DOWNLOAD_RETRIES | 2 | Integer 0–5 |
| TERRENCE_BINARY_DOWNLOAD_TIMEOUT_MS | 120000 | Integer 1–2147483647 |
| TERRENCE_BINARY_PROBE_TIMEOUT_MS | 10000 | Integer 1–2147483647 |
| TERRENCE_CSP_STRICT | false | true, false, 1, or 0 |
| TERRENCE_DB_IDLE_IN_TRANSACTION_TIMEOUT_MS | 60000 | Integer 0–86400000 |
| TERRENCE_DB_LOCK_TIMEOUT_MS | 10000 | Integer 0–86400000 |
| TERRENCE_DB_SLOW_QUERY_MS | 1000 | Integer 1–2147483647 |
| TERRENCE_DB_STATEMENT_TIMEOUT_MS | 30000 | Integer 0–86400000 |
| TERRENCE_DISABLE_RESTART | false | true, false, 1, or 0 |
| TERRENCE_DISABLE_WORKER | false | true, false, 1, or 0 |
| TERRENCE_DRAIN_GRACE_MS | 6000 | Integer 0–25000 |
| TERRENCE_ENABLE_LOCAL_SIGNUP | false | true, false, 1, or 0 |
| TERRENCE_EXPLAIN_TIMEOUT_MS | 60000 | Integer 1–2147483647 |
| TERRENCE_PASSWORD_DISALLOW_USERNAME | false | true, false, 1, or 0 |
| TERRENCE_PASSWORD_MIN_LENGTH | 10 | Integer 1–72 |
| TERRENCE_PASSWORD_REQUIRE_DIGIT | false | true, false, 1, or 0 |
| TERRENCE_PASSWORD_REQUIRE_LOWER | false | true, false, 1, or 0 |
| TERRENCE_PASSWORD_REQUIRE_SYMBOL | false | true, false, 1, or 0 |
| TERRENCE_PASSWORD_REQUIRE_UPPER | false | true, false, 1, or 0 |
| TERRENCE_QUERY_COUNT | false | true, false, 1, or 0 |
| TERRENCE_QUERY_LOG | false | true, false, 1, or 0 |
| TERRENCE_RECOVERY_RETENTION_MS | 604800000 | Integer 0–31536000000 |
| TERRENCE_RUN_CONCURRENCY | 5 | Integer 1–1024 |
| TERRENCE_SANDBOX_EXTRA_RW_ALLOWED | false | true, false, 1, or 0 |
| TERRENCE_SANDBOX_EXTRA_RW_ALLOW_STORAGE | false | true, false, 1, or 0 |
| TERRENCE_VERSION_CACHE_TTL_MS | 86400000 | Integer 1–31536000000 |
| TERRENCE_WORKER_POLL_MS | 1500 | Integer 100–2147483647 |

PUBLIC_URL accepts an HTTP(S) URL without credentials, query, or fragment.
CORS_ORIGIN accepts comma-separated HTTP(S) origins without paths; an empty value
means no explicit origin list. TERRENCE_RUN_SANDBOX accepts true/false and the
aliases 1/0, yes/no, on/off, and none (disabled). TERRENCE_RUN_NET_POLICY accepts
allow or deny; deny requires the sandbox. TERRENCE_EXECUTOR_BACKEND accepts
landlock, container, kubernetes, agent, or microvm and defaults to landlock.
TERRENCE_SANDBOX_MIN_ABI is optional; when set it must be an integer from 1 to 255.
Declaring an executor does not install it. PORT and SYSTEM_API_PORT must differ.

STORAGE_DIR defaults to backend/storage and must be a non-empty filesystem path.
SYSTEM_API_HOST defaults to 127.0.0.1. SYSTEM_API_TLS_CERT and SYSTEM_API_TLS_KEY
must be configured together as non-empty file paths; startup also checks that
the files exist and contain data. Remote host bindings require this TLS pair.
The existing container wildcard bindings 0.0.0.0 and :: remain supported.
Paths are redacted in the effective-configuration report.
TERRENCE_SANDBOX_EXTRA_RW_PATHS accepts at most 32 colon-separated absolute paths
and requires TERRENCE_SANDBOX_EXTRA_RW_ALLOWED. Paths that contain storage, lie
inside it, or equal it also require TERRENCE_SANDBOX_EXTRA_RW_ALLOW_STORAGE.

TERRENCE_TRUSTED_PROXY_CIDRS and TERRENCE_OUTBOUND_ALLOW_CIDRS accept comma-separated
IPv4 addresses or CIDRs. TERRENCE_OUTBOUND_ALLOW_HOSTS accepts comma-separated
hostnames. Each list accepts at most 256 entries and defaults to empty. Invalid
entries are rejected; IPv6 CIDRs are not supported by the current enforcement code.

GITHUB_API_URL defaults to https://api.github.com. GITHUB_APP_API_URL overrides
that base for GitHub App requests. GITHUB_APP_HTTP_URL is an optional web-base
override. These inputs accept HTTP(S) URLs without credentials, queries, or
fragments; empty explicit values are invalid. Their report values are redacted.

TERRENCE_NODE_STATUS accepts active (default), draining, or maintenance.
TERRENCE_AGENT_UPDATE_VERSION, TERRENCE_AGENT_UPDATE_URL, and
TERRENCE_AGENT_UPDATE_SHA256 must be configured together. The update URL must be
HTTP(S) without embedded credentials or a fragment; query parameters are allowed
for signed download URLs. SHA256 must contain exactly 64 hexadecimal characters.
The version accepts at most 128 letters, digits, dots, underscores, plus or minus
signs. An absent update triple disables update advertising; an invalid one fails
startup. Update URLs are redacted from the effective report.

SIGNED_URL_SECRET requires at least 32 characters; TERRENCE_TOKEN_HASH_SECRET
requires at least 32 UTF-8 bytes. Both retain their existing trimming behavior.
ENCRYPTION_PASSWORD is preserved verbatim and must be non-empty when supplied.
These secrets are limited to 65,536 characters (SIGNED_URL_SECRET) or UTF-8 bytes
(the other two), cannot contain NUL, require restart, and are always redacted.
Absent signing/hash secrets retain the existing persistent generated-key behavior;
an absent encryption password retains the installation-key behavior.

LOG_LEVEL defaults to info and accepts error, warn, info, or debug.
TERRENCE_SYSLOG_LEVEL inherits LOG_LEVEL. TERRENCE_SYSLOG_FORMAT accepts
rfc5424 (default) or json. TERRENCE_SYSLOG_TARGETS accepts up to 16 comma- or
newline-separated udp://host:port or tcp://host:port destinations. The legacy
TERRENCE_SYSLOG_TARGET alias is accepted; conflicting aliases are rejected.
TERRENCE_SYSLOG_HOSTNAME and TERRENCE_SYSLOG_APP accept printable ASCII without
spaces, at most 255 and 48 characters respectively. The app defaults to terrence;
the hostname is auto-detected when absent. These settings require restart.

Validated environment example:

```json
{
  "PUBLIC_URL": "https://terraform.example.com",
  "PORT": "3000",
  "SYSTEM_API_PORT": "8443",
  "TERRENCE_RUN_SANDBOX": "true",
  "TERRENCE_RUN_NET_POLICY": "deny",
  "TERRENCE_RUN_CONCURRENCY": "5"
}
```

## Persisted settings covered by the contract

Admin updates are validated before saving. Invalid submitted values return 422.
Invalid stored configuration prevents startup and returns 503 when encountered
during a settings read. Corrections are validated as a complete merged group.
Unknown fields within these groups are rejected.

| Setting | Accepted type or range | Report value |
| --- | --- | --- |
| approval-webhook.enabled | boolean | visible |
| approval-webhook.secret | string or null | redacted |
| approval-webhook.url | url or null | redacted |
| cost.aws-access-key-id | string or null | redacted |
| cost.aws-secret-key | string or null | redacted |
| cost.azure-client-id | string or null | redacted |
| cost.azure-client-secret | string or null | redacted |
| cost.azure-subscription-id | string or null | redacted |
| cost.azure-tenant-id | string or null | redacted |
| cost.enabled | boolean | visible |
| cost.gcp-credentials | json or null | redacted |
| cost.infracost-api-key | string or null | redacted |
| customization.footer | string or null | redacted |
| customization.login-help | string or null | redacted |
| customization.support-email-address | string or null | redacted |
| general.allow-speculative-plans-on-pull-requests-from-forks | boolean | visible |
| general.api-rate-limit | integer 1–1000000 | visible |
| general.api-rate-limiting-enabled | boolean | visible |
| general.apply-timeout | integer 1–604800 | visible |
| general.default-remote-state-access | boolean | visible |
| general.limit-user-organization-creation | boolean | visible |
| general.local-auth-enabled | boolean | visible |
| general.local-signup-enabled | boolean or null | visible |
| general.plan-timeout | integer 1–604800 | visible |
| general.send-passing-statuses-for-untriggered-speculative-plans | boolean | visible |
| general.trusted-client-ip-cidrs | cidrs | redacted |
| general.trusted-client-ip-headers | headers | visible |
| ldap.attr-display-name | string or null | redacted |
| ldap.attr-email | string or null | redacted |
| ldap.attr-username | string or null | redacted |
| ldap.base-dn | string or null | redacted |
| ldap.bind-dn | string or null | redacted |
| ldap.bind-password | string or null | redacted |
| ldap.enabled | boolean | visible |
| ldap.encryption | enum (plain, starttls, ldaps) | visible |
| ldap.host | string or null | redacted |
| ldap.link-by-email | boolean | visible |
| ldap.port | integer 1–65535 | visible |
| ldap.user-filter | string or null | redacted |
| logging.enabled | boolean or null | visible |
| logging.log-level | enum (error, warn, info, debug) or null | visible |
| logging.syslog-app | ascii or null | redacted |
| logging.syslog-format | enum (rfc5424, json) or null | visible |
| logging.syslog-hostname | ascii or null | redacted |
| logging.syslog-level | enum (error, warn, info, debug) or null | visible |
| logging.syslog-targets | syslog-targets or null | redacted |
| maintenance-windows.enabled | boolean | visible |
| maintenance-windows.windows | windows | redacted |
| oidc.client-id | string or null | redacted |
| oidc.client-secret | string or null | redacted |
| oidc.enabled | boolean | visible |
| oidc.issuer | issuer or null | redacted |
| oidc.link-by-email | boolean | visible |
| oidc.pkce-method | enum (S256, none) or null | visible |
| oidc.scopes | string or null | redacted |
| oidc.signing-alg | enum (HS256, HS384, HS512, RS256, RS384, RS512, ES256, ES384, ES512, PS256, PS384, PS512) or null | visible |
| plan-explainer.api-key | string or null | redacted |
| plan-explainer.base-url | url or null | redacted |
| plan-explainer.enabled | boolean | visible |
| plan-explainer.endpoint-url | url or null | redacted |
| plan-explainer.model | string or null | redacted |
| plan-explainer.provider | string or null | redacted |
| plan-explainer.reasoning-effort | enum (none, minimal, low, medium, high, xhigh, max) or null | visible |
| retention.delete-older-than-n-days | integer 1–36500 or null | visible |
| saml.link-by-email | boolean | visible |
| site.agent-enabled | boolean | visible |
| site.cost-estimation-enabled | boolean | visible |
| site.default-terraform-version | string | visible |
| site.max-run-timeout | integer 1–604800 | visible |
| site.module-registry-enabled | boolean | visible |
| site.opa-enabled | boolean | visible |
| site.provider-registry-enabled | boolean | visible |
| site.sentinel-enabled | boolean | visible |
| smtp.auth | enum (plain, login, none) | visible |
| smtp.enabled | boolean | visible |
| smtp.encryption | enum (starttls, tls, plain) | visible |
| smtp.host | string or null | redacted |
| smtp.password | string or null | redacted |
| smtp.port | integer 1–65535 | visible |
| smtp.sender-email | string or null | redacted |
| smtp.username | string or null | redacted |
| twilio.account-sid | string or null | redacted |
| twilio.auth-token | string or null | redacted |
| twilio.enabled | boolean | visible |
| twilio.from-number | string or null | redacted |

Enabled OIDC requires issuer and client-id. A client secret is required unless
using S256 without a symmetric signing algorithm. Enabled LDAP requires host and
base-dn; a service-account bind DN requires a bind password. OIDC issuer URLs must
use HTTPS, except HTTP on localhost or a loopback address, and cannot contain
credentials, queries, or fragments.

Validated general-settings attributes:

```json
{
  "plan-timeout": 3600,
  "apply-timeout": 3600,
  "api-rate-limit": 60
}
```

## Effective values

The admin-only GET /api/v2/admin/system-info response includes
deployment-configuration and persisted-configuration. Entries identify the value's
origin and whether a restart is required. Sensitive persisted values are redacted.
Persisted changes take effect on the next settings read; cache lifetime is at most
one second. Already running operations retain values captured when they started.
